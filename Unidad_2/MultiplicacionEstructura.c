#include <string.h>
struct table{
    int valor1;
    int valor2
    int resultado;
};
int main() {
    struct table arreglo[i] = {
          {1, 3, 0},
          {2, 3, 0},
          {3, 3, 0}
};
}
for (int i = 0; i<3; i++){
     int resultado = (arreglo[i].valor1)*(arreglo[i].valor2);
     arrglo[i].resultado = resultado;
};

for (int = 0; i<3; i++) {
    ptintf("%d*%d=%d\n",
          arreglo[i]valor1,
          arreglo[i]valor2,
          arreglo[i]resultado);
}
